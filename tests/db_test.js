// In review

// import { readFileSync } from "fs";
// import { readDB } from "../db/db_helper";

// jest.mock("fs");

// describe("Describe mock test", () => {
//   test("should read the db", () => {
//     const result = readDB();
//     readFileSync.mockImplementationOnce((_fd, buf, _pos, len, _offset) => {
//       buf.write("hello", "utf8");
//       return len;
//     });
//   });
// });
